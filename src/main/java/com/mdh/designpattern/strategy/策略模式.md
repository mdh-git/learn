# 策略模式

~~~
策略模式（Strategy Pattern）是一种行为设计模式，它定义了一系列算法，并将每个算法封装在独立的策略类中，使它们可以互相替换。

1.策略接口 (Strategy)：定义所有具体策略必须实现的公共接口。它规定了“怎么做”的契约。
2.具体策略 (Concrete Strategy)：实现策略接口，封装了具体的算法或行为。每一种算法都是一个独立的策略类。
3.上下文 (Context)：持有一个策略接口的引用，并提供统一的调用入口。它负责与具体的策略对象交互，但不关心策略的具体实现。
~~~