redis 实现分布式锁

redis 实现分布式锁的原理是基于 Redis 的 SETNX 命令，通过设置一个 key 的值来实现锁的功能。
SETNX key value         不能设置锁时间
set key value nx ex 30   原子性+过期时间，不会出现死锁

底层是通过Lua脚本实现（保证原子性）


Redisson实现分布式锁

RLock lock = redissonClient.getLock("key");
1.不开启Watch dog机制，到过期时间，锁会自动释放
    boolean isLocked = lock.tryLock(10, 30, TimeUnit.SECONDS);
2.开启Watch dog机制，每隔（releaseTime/3= 10s）的时间做一次续期  releaseTime：默认时间为30s
    boolean isLocked = lock.tryLock(10, TimeUnit.SECONDS);


Watch dog机制：
    自动延长锁的过期时间，保证只要持有锁的客户端业务还在执行（未主动释放锁 ），锁就不会因为过期而被释放，从而维持锁的有效性和互斥性。


工作原理与流程
    1.获取锁时的初始化
        当使用 Redisson 的 RLock（如 getLock 方法 ）获取分布式锁时，如果未手动指定锁的过期时间（即使用默认的 -1 ，表示锁的过期时间由看门狗机制管理 ），Redisson 会启动看门狗逻辑。
        它会先尝试用 setIfAbsent 命令向 Redis 设置锁（带一个初始的过期时间，默认是 30 秒 ，可通过 config.setLockWatchdogTimeout(...) 调整 ），如果设置成功，说明获取锁成功。
    2.后台定时任务续命
        获取锁成功后，Redisson 会在客户端后台启动一个定时任务（可以理解为一个守护线程 ），这个定时任务会周期性地（默认每隔 10 秒 ，是初始过期时间的 1/3 ，即 30/3 = 10 秒 ）检查当前客户端是否还持有锁（通过判断 Redis 中锁的 key 是否存在，且归属为当前客户端 ）。
        如果持有锁，就会向 Redis 发送一个 PEXPIRE 命令，将锁的过期时间重新设置为初始的过期时间（如 30 秒 ），实现“续命”。只要业务逻辑还在执行，这个定时任务就会不断给锁延长过期时间，保证锁不会因为超时被释放。
    3.锁释放时的清理
        当客户端主动调用 unlock 方法释放锁时，Redisson 会先校验锁的归属（确保是当前客户端持有的锁 ），然后删除 Redis 中的锁 key ，同时会停止后台的看门狗定时任务，避免不必要的续命操作。
        如果客户端因故障崩溃，无法主动释放锁，那么看门狗的定时任务也会随着客户端进程的结束而终止，锁在初始过期时间（或最后一次续命后的过期时间 ）到达后，会自动在 Redis 中过期释放，不会一直占用锁。

lockWatchdogTimeout：用于设置看门狗机制的初始锁过期时间（单位毫秒 ），默认是 30000 毫秒（30 秒 ）。
    可以在 Redisson 的配置类（如 Config ）中通过 setLockWatchdogTimeout 方法调整，
    比如：
        Config config = new Config();
        config.setLockWatchdogTimeout(60000); // 设置初始过期时间为 60 秒
        RedissonClient redisson = Redisson.create(config);



别的线程会while循环，不断地获取锁，直到获取到锁，或者超时（重试次数限制）。

Redisson实现锁的重入
    使用hash桀纣记录线程id和重入次数
    key：lock
    value：{"field":thread1, "value":2}


Redisson分布式锁-主从一致性问题（redis是ap思想，解决可用性，不能解决强一致性问题，只能实现最终一致性）（zookeeper是CP思想，可以保证强一致性）

    主从模式：master节点负责写数据，slave节点负责读数据。
    主从模式下，当master节点宕机，slave节点可以自动接管master节点，此刻master节点写数据并未同步到slave节点，这就可能导致数据不一致。

    RedLock（红锁）：不能只在一个redis实例上创建锁，应该是在多个redis实例上创建锁（n/2 + 1）,避免在一个redis实例上加锁
    问题：实现复杂、性能差、运维繁琐



