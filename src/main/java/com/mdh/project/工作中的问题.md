# 审图项目
~~~
最开始，刚入职的时候，审图、二次识图、规范判断是算法做的，对于业务数据全是黑盒的，最终审图报告都是cv返回的json，一层层取值展示到word、pdf

1.对现有的业务改造，保存算法最开始识图的数据（审图数据），把proto信息转成java对象，保存图纸信息到minio
2.原本由算法做二次识图功能，规范判断需要的变量
3.截图服务，针对预审截图，图框检测截图，多模态截图
    加载字体，
    InputStream is = this.getClass().getResourceAsStream("/fonts/simsun.ttc");
    Font font = Font.createFont(Font.TRUETYPE_FONT, is); 
    GraphicsEnvironment ge = GraphicsEnvironment.getLocalGraphicsEnvironment();
    ge.registerFont(font); // 注册后，就可以通过 new Font("SimSun", ...) 使用了
    
    
~~~