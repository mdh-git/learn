# 数组

~~~
数组（array）是一种用连续的内存空间存储相同数据结构类型数据的线性数据结构。

数组的下标从0开始
    寻址公式： base_address + index * data_type_size,计算下标的内存地址效率较高

查找的复杂度
    随机（通过下标）查询的时间复杂度是O(1)
    查找元素（未知下标）的时间复杂度为O(n)
    查找元素（未知下标但排序）通过二分查找的时间复杂度为O(log n)
    
插入删除的时间复杂度
    插入和删除的时候为了保证数组的连续性，需要移动数组元素，所以时间复杂度是O(n)
~~~